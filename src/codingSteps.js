// RoomFilter.js
import { useContext } from 'react';
import { RoomContext } from '../context';
import Title from '../components/Title';

const context = useContexct(RoomContext);
console.log(context);